{
  "name": "visualize-txt-traverse",
  "description": "visualize steps of txt-traverse.",
  "version": "0.0.1",
  "repository": {
    "url": "git://github.com/azu/visualize-txt-traverse.git"
  },
  "main": "app/app.js",
  "scripts": {
    "build": "browserify app/app.js -o public/app.js -v",
    "watch": "watchify app/app.js -o public/app.js -v",
    "test": "mocha test/*.js"
  },
  "directories": {
    "test": "test/"
  },
  "author": "azu",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/azu/visualize-txt-traverse/issues"
  },
  "dependencies": {
    "codemirror": "^4.0.3",
    "esprima": "^1.0.4",
    "markdown-to-ast": "^2.1.0",
    "txt-ast-traverse": "^1.2.0"
  },
  "devDependencies": {
    "watchify": "^2.2.1"
  }
}
